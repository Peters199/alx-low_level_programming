#include <stdio.h>
#include "main.h"
/**
* _putchar - write the char c to stdout
* @c:Th char to print
*
* Return: success on 1.
* On error, -1 is return and erno is set appropriately.
*/

int _putchar(char c)
{
return (write(1, &c, 1));
}
